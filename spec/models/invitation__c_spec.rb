require 'spec_helper'

describe "Invitation__c" do
	pending "add some description"
	describe "all" do
		context ":Start_Date__c" do
			it "should have a Start Date"
		end

		context ":End_Date__c" do
			it "should have a End Date"
		end

		context ":Text_Survey_Subject__c" do
			it "should have a Concept"
		end

		context ":User__c" do
			it "should have a User"
		end

		context ":Survey__c" do
			it "should have a Survey"
		end

		context ":Survey_Subject__c" do
			it "should have Subject"
		end
	end

end